name: CI

on: [push]

jobs:
  build:

    runs-on: macOS-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Build Electsys Utility.app
      run: xcodebuild build -workspace Electsys\ Utility.xcworkspace -scheme Electsys\ Utility
